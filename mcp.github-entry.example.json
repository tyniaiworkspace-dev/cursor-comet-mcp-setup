{
  "_comment": "Merge this 'github' entry into your %USERPROFILE%\\.cursor\\mcp.json under mcpServers. Replace YOUR_GITHUB_PAT with a Personal Access Token (repo scope).",
  "github": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env": {
      "GITHUB_PERSONAL_ACCESS_TOKEN": "YOUR_GITHUB_PAT"
    }
  }
}
