# Optional: use only if you need to pass env vars to MCP servers.
# Copy to .env and fill in; do not commit .env.
#
# Comet: usually no vars needed (wrapper sets COMET_TARGET_URL when a Space exists).
# poe2-optimizer: optional, e.g. POE2_MCP_DATA_DIR if you use a custom data path.
# POE2_MCP_DATA_DIR=
